# namma-recipe-
Features: browse recipes list, recipe detail (ingredient list + cooking steps), save/favorite recipes, share (native OS share + copy link), user auth (email+password + Google), offline caching for saved recipes, ingredient scaling (servings), unit conversion helper, search &amp; tag filters, and push notifications.
